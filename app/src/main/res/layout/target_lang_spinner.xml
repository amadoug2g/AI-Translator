<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="target"
            type="com.playgroundagc.core.data.TargetLang" />

        <import
            alias="Extension"
            type="com.playgroundagc.deepltranslator.util.ExtensionsKt" />
    </data>

    <androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/target_select_cardView"
        android:layout_width="140dp"
        android:layout_height="50dp"
        android:layout_margin="16dp"
        app:cardCornerRadius="26dp"
        app:cardElevation="4dp">

        <com.google.android.flexbox.FlexboxLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:padding="4dp"
            app:alignItems="center"
            app:justifyContent="space_evenly">

            <ImageView
                android:id="@+id/target_flag_img"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:contentDescription="@string/target_language_flag" />

            <TextView
                android:id="@+id/target_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="4dp"
                android:fontFamily="@font/ibm_plex_sans"
                android:maxLines="1"
                android:text="@{target.language}"
                android:textColor="@color/app_theme"
                android:textSize="13sp" />

        </com.google.android.flexbox.FlexboxLayout>

    </androidx.cardview.widget.CardView>
</layout>